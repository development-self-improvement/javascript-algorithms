function Queue() {

    var data = [];

    var add = function(element) {
        // Fill in-kk
    };

    var remove = function() {
        // Fill in
    };

    var find = function(value) {

    };

    var length = function() {
        // Fill in
    };

    return {
        add: add,
        remove: remove,
        length: length,
        find: find
    };
}